import { type FaqItem } from "../types/configDataTypes";

/** Fallback FAQ when Keystatic has no published items. Live FAQ lives in the CMS. */
export const faqData: FaqItem[] = [];

export default faqData;
