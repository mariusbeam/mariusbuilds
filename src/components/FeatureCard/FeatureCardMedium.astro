---
/**
 * Dark card with illustration, title, and text.
 * Matches the card style proof design.
 */

interface Props {
  illustration?: string;
  title: string;
  text: string;
}

const { illustration, title, text } = Astro.props as Props;
---

<div
  class="group flex flex-col items-start gap-5 rounded-2xl border border-white/[0.06] bg-base-900 p-8 transition-colors duration-200 hover:border-white/[0.12]"
>
  {illustration && (
    <div class="flex w-full items-center justify-center">
      <img src={illustration} alt="" class="h-32 w-auto" aria-hidden="true" />
    </div>
  )}

  <div class="flex flex-col gap-2">
    <h3 class="text-lg font-bold text-[#fdbb3e] leading-snug">{title}</h3>
    <p class="text-sm text-white/80 leading-relaxed">{text}</p>
  </div>
</div>
